import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  Medium: {
    fontFamily: "DM-M", // Ensure the font family matches the loaded font
  },
  Regular: {
    fontFamily: "D-Regular",
  },
  Bold: {
    fontFamily: "DM-Bold",
  },
  SemiBold: {
    fontFamily: "DM-Semi-Bold",
  },
});

export default styles;
